import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'aceternity.com',
      'images.unsplash.com',
      'images.pexels.com',
      'images.pexels.com',
      'images.unsplash.com',
      'cdn.sanity.io',
    ],
  },
};

export default nextConfig;
